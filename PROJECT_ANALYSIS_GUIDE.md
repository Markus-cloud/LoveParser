# Руководство по анализу проекта Tele-fluence Bot

## 📋 Обзор проекта

**Tele-fluence Bot** — это веб-приложение для парсинга Telegram-каналов и рассылки сообщений, разработанное как Telegram WebApp.

### Технологический стек:

**Frontend:**
- React 18 + TypeScript
- Vite (сборщик)
- React Router (маршрутизация)
- TanStack Query (управление состоянием сервера)
- shadcn/ui + Tailwind CSS (UI компоненты)
- Telegram WebApp SDK (интеграция с Telegram)

**Backend:**
- Express.js (API сервер)
- GramJS (Telegram Client Library)
- File-based storage (JSON файлы для данных)

---

## 🎯 План анализа проекта

### Этап 1: Общая архитектура (✅ Выполнено)

1. ✅ Структура проекта
   - Frontend: `src/`
   - Backend: `server/`
   - Конфигурация: корневая директория

2. ✅ Основные зависимости
   - Прочитан `package.json`
   - Понимание ключевых библиотек

3. ✅ Точки входа
   - Frontend: `src/main.tsx` → `App.tsx`
   - Backend: `server/index.js`

---

### Этап 2: Конфигурация и настройка

**2.1. Переменные окружения**

Проверьте наличие файла `.env` в корне проекта:
```env
API_PORT=4000
TELEGRAM_API_ID=your_api_id
TELEGRAM_API_HASH=your_api_hash
TELEGRAM_BOT_TOKEN=your_bot_token
```

**Действия:**
- [ ] Создать `.env` файл (если отсутствует)
- [ ] Заполнить Telegram API credentials
- [ ] Убедиться, что `.env` в `.gitignore`

**2.2. Установка зависимостей**

```bash
npm install
# или
bun install
```

---

### Этап 3: Backend анализ

**3.1. Структура сервера**

```
server/
├── index.js              # Точка входа Express сервера
├── lib/
│   ├── logger.js         # Логирование
│   ├── storage.js        # Работа с JSON файлами
│   ├── taskManager.js    # Менеджер фоновых задач
│   └── users.js          # Управление пользователями
├── routes/
│   ├── tasks.js          # API задач (GET, SSE stream)
│   ├── telegram.js       # Telegram операции (search, parse, broadcast)
│   ├── settings.js       # Настройки
│   └── user.js           # Авторизация пользователей
├── services/
│   └── telegramClient.js # GramJS клиент и операции
└── data/                 # JSON файлы (session, settings, tasks, users)
```

**3.2. Ключевые компоненты Backend:**

**API Endpoints:**
- `GET /api/health` - проверка здоровья
- `POST /api/telegram/search` - поиск каналов/чатов
- `POST /api/telegram/parse` - парсинг участников (фоновая задача)
- `POST /api/telegram/broadcast` - рассылка (фоновая задача)
- `GET /api/tasks` - список задач
- `GET /api/tasks/:id` - статус задачи
- `GET /api/tasks/:id/stream` - SSE поток прогресса
- `GET/POST /api/settings` - настройки
- `POST /api/user/login` - авторизация
- `GET /api/user/:id` - профиль пользователя

**Фоновые задачи:**
- `parse_audience` - парсинг активных участников канала
- `broadcast` - массовая рассылка сообщений

**Действия для анализа:**
- [ ] Изучить `server/services/telegramClient.js` - логика работы с Telegram
- [ ] Изучить `server/lib/taskManager.js` - система фоновых задач
- [ ] Проверить обработку ошибок в routes
- [ ] Изучить механизм авторизации (Telegram WebApp)

---

### Этап 4: Frontend анализ

**4.1. Структура Frontend**

```
src/
├── main.tsx              # Точка входа
├── App.tsx               # Главный компонент с роутингом
├── pages/
│   ├── Dashboard.tsx     # Главная страница
│   ├── Parsing.tsx       # Страница парсинга
│   ├── Audience.tsx      # Управление аудиторией
│   ├── Broadcast.tsx     # Рассылка сообщений
│   └── Help.tsx          # Помощь
├── components/
│   ├── Layout.tsx        # Общий layout
│   ├── Navigation.tsx    # Навигация
│   ├── GlassCard.tsx     # UI компонент
│   └── ui/               # shadcn/ui компоненты
├── context/
│   └── AuthContext.tsx   # Контекст авторизации
├── lib/
│   ├── api.ts            # API клиент
│   └── utils.ts          # Утилиты
└── hooks/                # React хуки
```

**4.2. Ключевые компоненты Frontend:**

**Роутинг:**
- `/` - Dashboard (главная)
- `/parsing` - Парсинг каналов
- `/audience` - Аудитория
- `/broadcast` - Рассылка
- `/help` - Помощь

**Авторизация:**
- Использует Telegram WebApp API (`window.Telegram.WebApp`)
- Автоматический вход через `initDataUnsafe.user`
- Сохранение пользователя в backend через `/api/user/login`

**API интеграция:**
- `src/lib/api.ts` - централизованный API клиент
- Автоматическое добавление `userId` в запросы
- Обработка ошибок

**Действия для анализа:**
- [ ] Изучить каждую страницу (`pages/*.tsx`)
- [ ] Проверить интеграцию с API
- [ ] Изучить использование Telegram WebApp SDK
- [ ] Проверить обработку состояний загрузки и ошибок
- [ ] Изучить UI компоненты и их использование

---

### Этап 5: Функциональность

**5.1. Основные функции:**

1. **Парсинг каналов:**
   - Поиск каналов через Telegram API
   - Парсинг активных участников за N дней
   - Сохранение результатов в JSON файлы

2. **Рассылка:**
   - Массовая рассылка сообщений
   - Отслеживание прогресса через SSE
   - Throttling для избежания flood limits

3. **Управление задачами:**
   - Создание фоновых задач
   - Отслеживание прогресса в реальном времени
   - История задач

4. **Авторизация:**
   - Интеграция с Telegram WebApp
   - Сохранение пользователей
   - Мультипользовательский режим (SaaS)

**Действия для анализа:**
- [ ] Протестировать поток парсинга (от поиска до сохранения)
- [ ] Протестировать поток рассылки
- [ ] Проверить работу SSE потоков
- [ ] Изучить систему подписок (Dashboard.tsx)

---

### Этап 6: Запуск и тестирование

**6.1. Запуск проекта:**

```bash
# Запуск frontend и backend одновременно
npm run dev:all

# Или отдельно:
npm run dev      # Frontend (Vite)
npm run server   # Backend (Express)
```

**6.2. Тестирование:**

1. **Проверка здоровья API:**
   ```bash
   curl http://localhost:4000/api/health
   ```

2. **Тестирование через Telegram:**
   - Запустить бота в Telegram
   - Открыть WebApp
   - Проверить авторизацию
   - Протестировать функции

3. **Проверка логов:**
   - Backend: консоль
   - Frontend: DevTools console

**Действия:**
- [ ] Убедиться, что сервер запускается без ошибок
- [ ] Проверить подключение к Telegram API
- [ ] Протестировать основные функции
- [ ] Проверить работу SSE потоков

---

### Этап 7: Безопасность и оптимизация

**7.1. Безопасность:**

- [ ] Проверить валидацию входных данных
- [ ] Проверить авторизацию на защищенных эндпоинтах
- [ ] Убедиться, что `.env` не попадает в Git
- [ ] Проверить CORS настройки
- [ ] Проверить rate limiting (throttling)

**7.2. Оптимизация:**

- [ ] Проверить размер бандла frontend
- [ ] Оптимизация запросов к Telegram API
- [ ] Проверить использование памяти (фоновые задачи)
- [ ] Проверить обработку больших списков участников

---

### Этап 8: Документация и код-стайл

**8.1. Документация:**

- [ ] README.md - обновлен и актуален
- [ ] SETUP.md - инструкции по настройке
- [ ] Комментарии в коде
- [ ] API документация (если нужно)

**8.2. Код-стайл:**

```bash
npm run lint        # Проверка ESLint
npm run check       # Линт + проверка зависимостей
```

- [ ] Проверить наличие ошибок линтера
- [ ] Исправить предупреждения
- [ ] Проверить форматирование кода

---

## 🚀 Быстрый старт для анализа

### Шаг 1: Установка и настройка
```bash
# 1. Установить зависимости
npm install

# 2. Создать .env файл
cp .env.example .env  # если есть пример
# Заполнить TELEGRAM_API_ID, TELEGRAM_API_HASH, TELEGRAM_BOT_TOKEN

# 3. Запустить проект
npm run dev:all
```

### Шаг 2: Изучение ключевых файлов (в порядке приоритета)

**Backend:**
1. `server/index.js` - структура API
2. `server/services/telegramClient.js` - работа с Telegram
3. `server/lib/taskManager.js` - система задач
4. `server/routes/telegram.js` - основные эндпоинты

**Frontend:**
1. `src/App.tsx` - роутинг
2. `src/context/AuthContext.tsx` - авторизация
3. `src/lib/api.ts` - API клиент
4. `src/pages/Dashboard.tsx` - главная страница
5. `src/pages/Parsing.tsx` - функция парсинга
6. `src/pages/Broadcast.tsx` - функция рассылки

### Шаг 3: Тестирование функций

1. Открыть приложение в Telegram WebApp
2. Протестировать поиск каналов
3. Протестировать парсинг участников
4. Протестировать рассылку сообщений

---

## 📝 Чеклист для полного анализа

### Конфигурация
- [ ] `.env` файл создан и заполнен
- [ ] Зависимости установлены
- [ ] Проект запускается без ошибок

### Backend
- [ ] Все API эндпоинты изучены
- [ ] Понимание работы Telegram клиента
- [ ] Понимание системы задач
- [ ] Проверка обработки ошибок
- [ ] Проверка авторизации

### Frontend
- [ ] Все страницы изучены
- [ ] Понимание роутинга
- [ ] Понимание авторизации
- [ ] Проверка интеграции с API
- [ ] Проверка UI компонентов

### Функциональность
- [ ] Парсинг работает корректно
- [ ] Рассылка работает корректно
- [ ] SSE потоки работают
- [ ] Прогресс задач отображается

### Безопасность
- [ ] Валидация входных данных
- [ ] Защита эндпоинтов
- [ ] `.env` в `.gitignore`
- [ ] CORS настройки корректны

### Документация
- [ ] README актуален
- [ ] SETUP.md актуален
- [ ] Код прокомментирован (где нужно)

---

## 🎓 Полезные команды

```bash
# Разработка
npm run dev          # Frontend только
npm run server       # Backend только
npm run dev:all      # Оба одновременно

# Сборка
npm run build        # Production build
npm run preview      # Preview production build

# Проверка
npm run lint         # ESLint проверка
npm run check        # Линт + проверка зависимостей
npm run test:all     # Полная проверка

# Тестирование зависимостей
npm run test:deps    # Проверка установленных зависимостей
```

---

## 🔍 Следующие шаги после анализа

После завершения анализа вы можете:

1. **Добавить новые функции:**
   - Экспорт данных в различные форматы
   - Фильтры для парсинга
   - Шаблоны для рассылок
   - Статистика и аналитика

2. **Улучшить существующий код:**
   - Рефакторинг
   - Оптимизация производительности
   - Улучшение UI/UX
   - Добавление тестов

3. **Исправить найденные проблемы:**
   - Безопасность
   - Обработка ошибок
   - Валидация данных

4. **Добавить инфраструктуру:**
   - База данных (вместо JSON файлов)
   - Redis для задач
   - Мониторинг и логирование
   - CI/CD

---

## 📚 Дополнительные ресурсы

- [GramJS Documentation](https://docs.telethon.dev/)
- [Telegram WebApp API](https://core.telegram.org/bots/webapps)
- [React Router Docs](https://reactrouter.com/)
- [TanStack Query Docs](https://tanstack.com/query/latest)
- [shadcn/ui Components](https://ui.shadcn.com/)

---

**Дата создания:** $(date)
**Версия:** 1.0.0

