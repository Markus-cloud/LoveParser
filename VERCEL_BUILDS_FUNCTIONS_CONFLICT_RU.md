# Исправление конфликта functions и builds в Vercel

## Проблема

При деплое на Vercel возникла ошибка:
```
The `functions` property cannot be used in conjunction with the `builds` property. Please remove one of them.
```

Ошибка означает, что в конфигурационном файле `vercel.json` одновременно использовались два взаимоисключающих свойства.

## Анализ конфигурации

### 1. Структура проекта

```
/
├── api/
│   └── index.js          # Точка входа для serverless функции
├── server/
│   ├── index.js          # Express приложение
│   └── routes/           # API роутеры
├── src/                  # Исходники React фронтенда
├── dist/                 # Собранный фронтенд (после npm run build)
├── package.json
└── vercel.json           # Конфигурация деплоя
```

### 2. Архитектура приложения

- **Frontend**: React 18 + Vite, собирается в `dist/`
- **Backend**: Express приложение, экспортируемое как Vercel serverless функция
- **API Entry Point**: `api/index.js` импортирует и экспортирует Express app
- **Routing**: Все запросы к `/api/*` обрабатываются serverless функцией

### 3. Источник конфликта

В файле `vercel.json` одновременно присутствовали:

**Устаревшая конфигурация (v2 legacy):**
- `builds` - явное определение шагов сборки
- `routes` - ручная маршрутизация URL

**Современная конфигурация:**
- `functions` - настройка serverless функций
- `rewrites` - переписывание URL

Vercel не позволяет использовать оба подхода одновременно, так как это создаёт неоднозначность в процессе деплоя.

## Решение

Удалены устаревшие свойства `builds` и `routes`, конфигурация переведена на современный стандарт Vercel.

### До исправления (с конфликтом):

```json
{
  "version": 2,
  "builds": [                          // ❌ Конфликтует с functions
    {
      "src": "api/index.js",
      "use": "@vercel/node"
    },
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    }
  ],
  "routes": [                          // ⚠️ Устаревший подход
    {
      "src": "/api/(.*)",
      "dest": "/api/index.js"
    },
    {
      "src": "/(.*)",
      "dest": "/dist/$1"
    }
  ],
  "functions": {                       // ✅ Современный подход
    "api/index.js": {
      "maxDuration": 30
    }
  },
  "env": {
    "NODE_ENV": "production"
  }
}
```

### После исправления (без конфликта):

```json
{
  "version": 2,
  "buildCommand": "npm run build",     // ✅ Явная команда сборки
  "outputDirectory": "dist",           // ✅ Директория статических файлов
  "functions": {                       // ✅ Настройка функций
    "api/index.js": {
      "maxDuration": 30
    }
  },
  "rewrites": [                        // ✅ Современная маршрутизация
    {
      "source": "/api/:path*",
      "destination": "/api/index.js"
    }
  ],
  "env": {
    "NODE_ENV": "production"
  }
}
```

## Ключевые изменения

### 1. ❌ Удалено: `builds`

**Причина:** Vercel теперь автоматически определяет serverless функции в директории `api/`.

**Преимущества:**
- Нет необходимости явно указывать `@vercel/node` и другие билдеры
- Упрощённая конфигурация
- Меньше кода для поддержки

### 2. ✅ Добавлено: `buildCommand` и `outputDirectory`

**Назначение:**
- `buildCommand: "npm run build"` - команда для сборки фронтенда
- `outputDirectory: "dist"` - где искать статические файлы после сборки

**Преимущества:**
- Явное указание процесса сборки
- Понятная структура конфигурации
- Лучшая читаемость

### 3. ❌ Удалено: `routes`, ✅ Добавлено: `rewrites`

**Причина:** `rewrites` - современная замена для `routes`.

**Как работает:**
```
Запрос: /api/telegram/auth
   ↓
Rewrite: /api/index.js (serverless функция)
   ↓
Express: server/routes/telegram.js
   ↓
Ответ
```

### 4. ✅ Сохранено: `functions`

**Назначение:** Настройка параметров serverless функций.

**Параметры:**
- `maxDuration: 30` - максимальное время выполнения 30 секунд
  - Необходимо для длительных операций с Telegram API
  - Важно для получения списка участников больших каналов

## Процесс деплоя

### 1. Фаза сборки (Build Phase)

```bash
npm run build
# Vite собирает фронтенд в dist/
```

### 2. Определение функций (Function Detection)

```
api/index.js → Автоматически определяется как serverless функция
```

Vercel автоматически:
- Находит все `.js` файлы в `api/`
- Создаёт для них serverless функции
- Настраивает маршрутизацию

### 3. Runtime маршрутизация

```
/api/*        → api/index.js (serverless)
/*            → dist/ (статические файлы)
```

## Проверка конфигурации

### Автоматическая валидация

```bash
# Проверка конфликтов и корректности конфигурации
node validate-vercel-config.js

# Проверка API handler
node test-vercel-api.js

# Проверка чистоты конфигурации (нет устаревших файлов)
node verify-vercel-config.js
```

### Ручное тестирование

```bash
# Тест сборки фронтенда
npm run build

# Локальный запуск сервера
npm run server

# Полный стек локально
npm run dev:all
```

### Деплой на Vercel

```bash
# Preview деплой
vercel

# Production деплой
vercel --prod
```

## Технические детали

### Почему возникает конфликт?

Vercel эволюционировал от явной конфигурации к автоматическому определению:

| Подход | Период | Описание |
|--------|--------|----------|
| **Legacy (v1-v2)** | Старый | Требует явный массив `builds` с билдерами |
| **Modern (v2+)** | Текущий | Автоматическое определение + `functions` для настройки |

Смешивание подходов создаёт конфликт интерпретации конфигурации.

### Автоматическое определение функций

Vercel автоматически создаёт serverless функции для файлов в `api/`:

```
api/index.js   → /api/index (serverless функция)
api/users.js   → /api/users (serverless функция)
api/health.js  → /api/health (serverless функция)
```

В нашем случае:
- `api/index.js` экспортирует полное Express приложение
- Express самостоятельно обрабатывает все маршруты `/api/*`

### Конфигурация функций

```json
"functions": {
  "api/index.js": {
    "maxDuration": 30,     // Макс. время выполнения в секундах
    "memory": 1024,        // Память в MB (опционально)
    "runtime": "nodejs18.x" // Node.js версия (опционально)
  }
}
```

## Результаты

### ✅ Что исправлено:

1. **Конфликт устранён** - удалено свойство `builds`
2. **Современная конфигурация** - используются актуальные подходы Vercel
3. **Упрощённая структура** - меньше строк кода, понятнее логика
4. **Готовность к деплою** - все проверки проходят успешно

### ✅ Проверки пройдены:

```
✅ vercel.json существует и валидный
✅ Нет конфликта builds/functions
✅ api/index.js существует
✅ Нет устаревших конфигурационных файлов
✅ .gitignore правильно настроен
✅ Сборка фронтенда успешна
✅ API handler корректно настроен
✅ Все роуты зарегистрированы
```

### 📋 Следующие шаги:

1. **Деплой preview версии:**
   ```bash
   vercel
   ```

2. **Проверка функциональности:**
   - Тест endpoint: `https://your-app.vercel.app/api/health`
   - Проверка фронтенда: `https://your-app.vercel.app/`
   - Тест авторизации Telegram

3. **Production деплой:**
   ```bash
   vercel --prod
   ```

4. **Настройка переменных окружения в Vercel Dashboard:**
   - `TELEGRAM_API_ID`
   - `TELEGRAM_API_HASH`

## Дополнительная информация

### Переменные окружения

Настройте в Vercel Dashboard:
- `TELEGRAM_API_ID` - ID вашего Telegram приложения
- `TELEGRAM_API_HASH` - Hash вашего Telegram приложения

### Node.js версия

Указана в `package.json`:
```json
"engines": {
  "node": ">=18.0.0"
}
```

### Timeout функций

Установлен 30 секунд для API функций:
- Достаточно для операций с Telegram API
- Позволяет обрабатывать большие каналы
- Стандартный лимит Vercel Hobby плана

### CORS

Настроен в Express приложении:
```javascript
app.use(cors({ origin: '*' }));
```

## Документация

Созданы следующие документы:

1. **VERCEL_CONFIG_FIX.md** (EN) - Детальное описание исправления
2. **VERCEL_BUILDS_FUNCTIONS_CONFLICT_RU.md** (RU) - Этот документ
3. **validate-vercel-config.js** - Скрипт проверки конфигурации

## Ссылки

- [Vercel Functions Documentation](https://vercel.com/docs/functions)
- [Vercel Configuration Reference](https://vercel.com/docs/project-configuration)
- [Migrating from Builds to Functions](https://vercel.com/docs/functions/configuring-functions)

---

**Дата исправления:** 2024  
**Статус:** ✅ Готово к деплою  
**Версия конфигурации:** Modern Vercel v2
