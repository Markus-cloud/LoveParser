# Local development environment variables
# Copy this file to .env.local and fill in your actual values
# IMPORTANT: Keep .env.local secure and never commit to Git!

# Server Configuration
API_PORT=4000

# Optional: Additional allowed CORS origins (comma-separated)
# By default, localhost:*, 127.0.0.1:*, and ngrok domains are allowed
# Use this to add additional origins for testing/deployment
# Example: ALLOWED_ORIGINS=https://example.com,https://app.example.com
# ALLOWED_ORIGINS=

# Telegram API Configuration (REQUIRED for auth to work)
# Get these from https://my.telegram.org/apps
TELEGRAM_API_ID=your_api_id_here
TELEGRAM_API_HASH=your_api_hash_here

# Telegram Bot Token (REQUIRED for avatar downloads via Bot API)
# Get this from @BotFather on Telegram
# Without this token, user avatars cannot be downloaded
TELEGRAM_BOT_TOKEN=your_bot_token_here

# Node environment
NODE_ENV=development