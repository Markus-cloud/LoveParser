# 🧪 Инструкция по тестированию зависимостей

## Быстрая проверка (рекомендуется)

Выполните последовательно следующие команды:

### 1. Установка зависимостей
```bash
npm install
```

### 2. Проверка зависимостей
```bash
npm run test:deps
```

### 3. Проверка линтера
```bash
npm run lint
```

### 4. Проверка сборки фронтенда
```bash
npm run build
```

### 5. Проверка запуска сервера (в отдельном терминале)
```bash
npm run server
```
Сервер должен запуститься на `http://localhost:4000` и показать сообщение:
```
[server] Listening on http://localhost:${PORT}
```

### 6. Проверка API endpoints
После запуска сервера, проверьте health endpoint:
```bash
curl http://localhost:4000/api/health
```
Должен вернуть: `{"ok":true,"service":"tele-fluence-backend"}`

### 7. Проверка фронтенда (в отдельном терминале)
```bash
npm run dev
```
Фронтенд должен запуститься на `http://localhost:8080`

---

## Полная проверка одной командой

Выполните все проверки сразу:
```bash
npm run test:all
```

Эта команда выполнит:
- ✅ Линтинг кода
- ✅ Сборку проекта
- ✅ Проверку зависимостей

---

## Детальная проверка вручную

### Проверка серверных модулей

Создайте файл `test-server-imports.js`:

```javascript
import express from 'express';
import cors from 'cors';
import dotenv from 'dotenv';
import bodyParser from 'body-parser';
import { TelegramClient } from 'telegram';
import { v4 as uuidv4 } from 'uuid';

console.log('✅ Все серверные модули успешно импортированы!');
console.log('- express:', typeof express);
console.log('- cors:', typeof cors);
console.log('- dotenv:', typeof dotenv);
console.log('- bodyParser:', typeof bodyParser);
console.log('- TelegramClient:', typeof TelegramClient);
console.log('- uuid:', typeof uuidv4);
```

Запустите:
```bash
node test-server-imports.js
```

### Проверка TypeScript типов

```bash
npx tsc --noEmit
```

### Проверка структуры проекта

Убедитесь, что все файлы существуют:
- ✅ `server/index.js`
- ✅ `server/routes/*.js`
- ✅ `server/lib/*.js`
- ✅ `server/services/*.js`
- ✅ `src/**/*.tsx`
- ✅ `vite.config.ts`
- ✅ `tsconfig.json`

---

## Проверка работы API

После запуска сервера (`npm run server`), проверьте endpoints:

### 1. Health check
```bash
curl http://localhost:4000/api/health
```

### 2. User login (пример)
```bash
curl -X POST http://localhost:4000/api/user/login \
  -H "Content-Type: application/json" \
  -d '{"user":{"id":"123","username":"test"}}'
```

### 3. Settings
```bash
curl http://localhost:4000/api/settings
```

---

## Проверка фронтенда

После запуска фронтенда (`npm run dev`):

1. Откройте `http://localhost:8080` в браузере
2. Проверьте консоль браузера (F12) на наличие ошибок
3. Проверьте Network tab - запросы к `/api/*` должны проходить через прокси

---

## Возможные проблемы и решения

### ❌ Ошибка: "Cannot find module 'body-parser'"
**Решение:** `npm install`

### ❌ Ошибка: "Port 4000 already in use"
**Решение:** Измените `API_PORT` в `.env` или остановите процесс на порту 4000

### ❌ Ошибка: "Cannot find module '@/components/Layout'"
**Решение:** Проверьте `tsconfig.json` и `vite.config.ts` - путь `@` должен указывать на `./src`

### ❌ Ошибка: "Failed to fetch /api/health"
**Решение:** 
- Убедитесь, что сервер запущен
- Проверьте прокси в `vite.config.ts`
- Проверьте CORS настройки в `server/index.js`

### ❌ Ошибка: "TypeError: Cannot read property 'theme' of undefined"
**Решение:** Уже исправлено - `useTheme` удален из `sonner.tsx`

---

## Чеклист перед деплоем

- [ ] `npm install` выполнен без ошибок
- [ ] `npm run lint` не показывает ошибок
- [ ] `npm run build` успешно собирает проект
- [ ] `npm run server` запускается без ошибок
- [ ] API endpoints отвечают корректно
- [ ] Фронтенд открывается в браузере
- [ ] Нет ошибок в консоли браузера
- [ ] Все переменные окружения настроены (`.env`)

---

## Автоматическая проверка

Используйте созданный скрипт для автоматической проверки:

```bash
npm run test:deps
```

Скрипт проверит:
- ✅ Установку всех зависимостей
- ✅ Наличие всех файлов проекта
- ✅ Корректность конфигурационных файлов
- ✅ Доступность импортов

---

**Успешного тестирования! 🚀**

