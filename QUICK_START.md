# ⚡ Быстрый старт анализа проекта

## 📦 Шаг 1: Подготовка (2 минуты)

```bash
# 1. Установить зависимости
npm install

# 2. Создать .env файл (если его нет)
# Создайте файл .env в корне проекта:
```

`.env` файл:
```env
API_PORT=4000
TELEGRAM_API_ID=ваш_api_id
TELEGRAM_API_HASH=ваш_api_hash
TELEGRAM_BOT_TOKEN=ваш_токен_бота
```

> **Как получить credentials:** См. [SETUP.md](./SETUP.md)

## 🚀 Шаг 2: Запуск (1 минута)

```bash
# Запустить frontend + backend одновременно
npm run dev:all
```

**Или отдельно:**
```bash
npm run dev      # Frontend на http://localhost:5173
npm run server   # Backend на http://localhost:4000
```

## 📖 Шаг 3: Изучение (30-60 минут)

### 🔴 Приоритет 1: Основная архитектура

**Backend (10 мин):**
```
server/index.js                 → Структура Express API
server/services/telegramClient.js → Работа с Telegram
server/lib/taskManager.js        → Фоновые задачи
server/routes/telegram.js        → Основные эндпоинты
```

**Frontend (10 мин):**
```
src/App.tsx                     → Роутинг и структура
src/context/AuthContext.tsx     → Авторизация
src/lib/api.ts                  → API клиент
```

### 🟡 Приоритет 2: Функциональность

**Страницы (15 мин):**
```
src/pages/Dashboard.tsx  → Главная страница
src/pages/Parsing.tsx    → Парсинг каналов
src/pages/Audience.tsx   → Управление аудиторией
src/pages/Broadcast.tsx  → Рассылка сообщений
```

**Компоненты (10 мин):**
```
src/components/Layout.tsx
src/components/Navigation.tsx
src/components/GlassCard.tsx
```

### 🟢 Приоритет 3: Детали (по необходимости)

- `server/lib/storage.js` - работа с файлами
- `server/lib/logger.js` - логирование
- `server/routes/tasks.js` - API задач
- `server/routes/settings.js` - настройки

## 🧪 Шаг 4: Тестирование (10 минут)

1. **Проверить API:**
   ```bash
   curl http://localhost:4000/api/health
   ```

2. **Открыть в браузере:**
   - Frontend: http://localhost:5173
   - Backend: http://localhost:4000/api/health

3. **Тестировать в Telegram:**
   - Запустить бота
   - Открыть WebApp
   - Проверить функции

## 📋 Быстрый чеклист

- [ ] Зависимости установлены (`npm install`)
- [ ] `.env` файл создан и заполнен
- [ ] Сервер запускается (`npm run dev:all`)
- [ ] API отвечает (`/api/health`)
- [ ] Frontend открывается в браузере
- [ ] Изучены основные файлы
- [ ] Понимание архитектуры

## 🎯 Ключевые моменты для понимания

### 1. Авторизация
- Использует Telegram WebApp API
- Автоматический вход через `window.Telegram.WebApp`
- `userId` передается в каждый API запрос

### 2. Фоновые задачи
- Используется `TaskManager` для долгих операций
- Прогресс отслеживается через SSE (`/api/tasks/:id/stream`)
- Задачи: `parse_audience`, `broadcast`

### 3. Telegram API
- Используется GramJS библиотека
- Сессия сохраняется в `server/data/session.json`
- Throttling: 700-800ms между запросами

### 4. Хранение данных
- JSON файлы в `server/data/`
- `settings.json` - настройки
- `session.json` - сессия Telegram
- `tasks.json` - задачи
- `users_<chatId>.json` - спарсенные участники

## 🐛 Типичные проблемы

**Проблема:** "Telegram settings missing"
- **Решение:** Проверьте `.env` файл, заполните `TELEGRAM_API_ID` и `TELEGRAM_API_HASH`

**Проблема:** Сервер не запускается
- **Решение:** Проверьте порт 4000, убедитесь что зависимости установлены

**Проблема:** Frontend не подключается к API
- **Решение:** Проверьте `VITE_API_URL` в `.env` или используйте прокси в `vite.config.ts`

**Проблема:** Ошибка авторизации Telegram
- **Решение:** Проверьте правильность `TELEGRAM_API_ID`, `TELEGRAM_API_HASH`, `TELEGRAM_BOT_TOKEN`

## 📚 Полезные ссылки

- **Полное руководство:** [PROJECT_ANALYSIS_GUIDE.md](./PROJECT_ANALYSIS_GUIDE.md)
- **Настройка:** [SETUP.md](./SETUP.md)
- **README:** [README.md](./README.md)

## 🎓 Что дальше?

После изучения основ:
1. Изучите конкретную функцию, которую хотите улучшить
2. Проверьте обработку ошибок
3. Изучите UI компоненты (shadcn/ui)
4. Поэкспериментируйте с API эндпоинтами

---

**Время на полный анализ:** ~1-2 часа
**Время на быстрый обзор:** ~30 минут

